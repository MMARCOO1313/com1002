# SmartGate kiosk dependencies
# Optional legacy support: install `face-recognition` manually if a local dlib build is available.

mediapipe>=0.10.0
opencv-python>=4.9.0
pillow>=10.0.0
requests>=2.31.0
numpy>=1.24.0
